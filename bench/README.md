# bench/ — AstroTracker benchmark harness (P0-07)

Root-level dev-tooling workspace member (`@astrotracker/bench`), peer to `fixtures/` per
DD-002's module layout — not an app package under `packages/`.

This package is being built out across docs/plans/p0-07-benchmark-harness.md's steps; this
file is filled in with the full benchmark/gate/baseline-update documentation in that plan's
Step 9.
