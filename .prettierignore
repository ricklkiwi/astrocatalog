node_modules/
dist/
coverage/
pnpm-lock.yaml
*.tsbuildinfo

# Claude Code's transient worktree/scratch area (parallel-agent copies, built
# `out/`, etc.) — never part of the linted source tree, untracked, and absent
# in CI. Scoped narrowly so tracked files under .claude/ (e.g. agents/*.md)
# stay covered by prettier --check.
.claude/worktrees/

# Fixture manifests are generated byte-exactly by fixtures/src/author.ts;
# reformatting them would break the byte-determinism guard (P0-06).
fixtures/fits/manifest.json
fixtures/xisf/manifest.json
fixtures/raw/manifest.json
fixtures/generated/
