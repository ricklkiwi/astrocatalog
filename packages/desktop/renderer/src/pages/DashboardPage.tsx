import { PlaceholderPage } from '../app/PlaceholderPage';

export function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="Library totals, integration time this month, recent sessions, targets by status, and calibration health warnings will appear here."
    />
  );
}
