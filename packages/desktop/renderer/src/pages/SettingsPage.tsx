import { PlaceholderPage } from '../app/PlaceholderPage';

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Watch folders, timezone and site settings, matching tolerances, session gap, and theme preferences will appear here."
    />
  );
}
