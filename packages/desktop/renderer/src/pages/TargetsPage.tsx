import { PlaceholderPage } from '../app/PlaceholderPage';

export function TargetsPage() {
  return (
    <PlaceholderPage
      title="Targets"
      description="Search, filter, and browse every target in your library will appear here."
    />
  );
}
