import { PlaceholderPage } from '../app/PlaceholderPage';

export function SessionsPage() {
  return (
    <PlaceholderPage
      title="Sessions"
      description="A calendar heat-map and chronological list of every imaging session will appear here."
    />
  );
}
