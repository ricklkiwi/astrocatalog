import { PlaceholderPage } from '../app/PlaceholderPage';

export function CalibrationPage() {
  return (
    <PlaceholderPage
      title="Calibration"
      description="Master calibration candidates, gap reports, and ranked matching suggestions will appear here."
    />
  );
}
