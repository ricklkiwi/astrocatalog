# Specs

This folder is for active implementation specs only.

A spec is active while its task is in implementation or review. Once the reviewer returns PASS, the orchestrator must move it with its plan to:

```text
docs/archive/tasks/<slug>/spec.md
```

Do not leave completed task specs here for reference. Use the archive index instead:

- [Archived task documents](../archive/tasks/README.md)
